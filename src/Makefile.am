
AM_CFLAGS = --pedantic -std=gnu99 -lm -pipe -O2 -Wall -W -D_REENTRANT
AM_LDFLAGS =

# this lists the binaries to produce, the (non-PHONY, binary) targets in
# the previous manual Makefile
bin_PROGRAMS = mkrand
mkrand_SOURCES = cell_logic.c machine_logic.c fmt_vec.c command.c
